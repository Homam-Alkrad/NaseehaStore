@{
    ViewBag.Title = "نجاح الطلب";
}
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <link href="~/css/site.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }

        .order-success-container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: right;
            min-height: 100vh;
            padding: 20px;
        }

            .order-success-container h1 {
                font-size: 2.5rem;
                color: #28a745;
                margin-bottom: 1rem;
            }

            .order-success-container p {
                font-size: 1.2rem;
                color: #555;
                margin-bottom: 2rem;
            }

            .order-success-container .btn {
                display: inline-block;
                background-color: #007bff;
                color: #fff;
                padding: 10px 20px;
                text-decoration: none;
                font-size: 1rem;
                border-radius: 5px;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
                transition: background-color 0.3s ease, transform 0.2s ease;
            }

                .order-success-container .btn:hover {
                    background-color: #0056b3;
                    transform: translateY(-2px);
                }

            .order-success-container .success-icon {
                font-size: 5rem;
                color: #28a745;
                margin-bottom: 1.5rem;
            }

            .order-success-container .order-details {
                background-color: #fff;
                border: 1px solid #ddd;
                border-radius: 8px;
                padding: 20px;
                max-width: 400px;
                width: 100%;
                margin-bottom: 2rem;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
                text-align: right;
            }

                .order-success-container .order-details p {
                    font-size: 1rem;
                    margin-bottom: 0.5rem;
                    color: #333;
                }

                    .order-success-container .order-details p span {
                        font-weight: bold;
                        color: #007bff;
                    }
    </style>
</head>
<body>
    <div class="order-success-container">
        <h1>شكرًا على طلبك!</h1>
        <p>تم تقديم طلبك بنجاح.</p>

        <div class="order-details">
            <p>رقم الطلب: <span>@ViewBag.OrderNumber</span></p>
            <p>اسم الدورة: <span>@ViewBag.CourseName</span></p>
            <p>وصف الدورة: <span style="font-size:10px;">@ViewBag.CourseDescription</span></p>
            <p>إجمالي السعر: <span>@ViewBag.TotalPrice دينار</span></p>
        </div>

        <a href="/Home/Shop" class="btn">العودة إلى المتجر</a>
    </div>
</body>
</html>
